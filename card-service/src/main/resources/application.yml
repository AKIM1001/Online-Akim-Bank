server:
  port: 8083

springdoc:
  swagger-ui:
    path: /swagger-ui.html

spring:
  application:
    name: card-service
  threads:
    virtual:
      enabled: true

keycloak:
  use-resource-role-mappings: true

management:
  endpoints:
    web:
      exposure:
        include: prometheus