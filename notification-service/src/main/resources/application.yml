server:
  port: 8087

springdoc:
  swagger-ui:
    path: /swagger-ui.html

spring:
  application:
    name: notification-service
  threads:
    virtual:
      enabled: true

keycloak:
  use-resource-role-mappings: true

management:
  endpoints:
    web:
      exposure:
        include: prometheus