spring:
  cloud:
    vault:
      enabled: true
      uri: http://127.0.0.1:8200
      authentication: TOKEN
      token: ${VAULT_TOKEN}
      kv:
        enabled: true
        backend: secret
        application-name: dev/api-gateway
