server:
  port: 8085

springdoc:
  swagger-ui:
    path: /swagger-ui.html

spring:
  application:
    name: transaction-service
  threads:
    virtual:
      enabled: true

keycloak:
  use-resource-role-mappings: true

management:
  endpoints:
    web:
      exposure:
        include: prometheus