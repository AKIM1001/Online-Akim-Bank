server:
  port: 8082

springdoc:
  swagger-ui:
    path: /swagger-ui.html

spring:
  application:
    name: account-service
  threads:
    virtual:
      enabled: true

keycloak:
  use-resource-role-mappings: true

management:
  endpoints:
    web:
      exposure:
        include: prometheus