spring:
  config:
    import: vault://secret/dev/loan-service
  cloud:
    vault:
      uri: http://localhost:8200
      authentication: TOKEN
      token: hvstoken123
      kv:
        enabled: true
        backend: secret
        application-name: dev/loan-service
      enabled: true