apiVersion: apps/v1
kind: Deployment
metadata:
  name: payment-service
  namespace: onlineakimbank
spec:
  replicas: 1
  selector:
    matchLabels:
      app: payment-service
  template:
    metadata:
      labels:
        app: payment-service
    spec:
      initContainers:
        - name: wait-for-eureka
          image: curlimages/curl:latest
          command:
            - sh
            - -c
            - 'until curl -s http://eureka-server:8761/eureka/apps > /dev/null; do echo "Waiting for Eureka Server..."; sleep 5; done'
      containers:
        - name: payment-service
          imagePullPolicy: Always
          image: ghcr.io/akim1001/onlineakimbank/payment-service:latest
          ports:
            - containerPort: 8084
          env:
            - name: SPRING_PROFILES_ACTIVE
              value: "prod"
      imagePullSecrets:
        - name: ghcr-secret
---
apiVersion: v1
kind: Service
metadata:
  name: payment-service
  namespace: onlineakimbank
spec:
  selector:
    app: payment-service
  ports:
    - port: 8084
      targetPort: 8084
