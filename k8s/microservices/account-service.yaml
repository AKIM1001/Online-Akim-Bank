apiVersion: apps/v1
kind: Deployment
metadata:
  name: account-service
  namespace: onlineakimbank
spec:
  replicas: 1
  selector:
    matchLabels:
      app: account-service
  template:
    metadata:
      labels:
        app: account-service
    spec:
      initContainers:
        - name: wait-for-eureka
          image: curlimages/curl:latest
          command:
            - sh
            - -c
            - 'until curl -s http://eureka-server:8761/eureka/apps > /dev/null; do echo "Waiting for Eureka Server..."; sleep 5; done'
      containers:
        - name: account-service
          imagePullPolicy: Always
          image: ghcr.io/akim1001/onlineakimbank/account-service:latest
          ports:
            - containerPort: 8082
          env:
            - name: SPRING_PROFILES_ACTIVE
              value: "prod"
      imagePullSecrets:
        - name: ghcr-secret
---
apiVersion: v1
kind: Service
metadata:
  name: account-service
  namespace: onlineakimbank
spec:
  selector:
    app: account-service
  ports:
    - port: 8082
      targetPort: 8082
