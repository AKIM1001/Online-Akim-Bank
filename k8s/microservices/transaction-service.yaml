apiVersion: apps/v1
kind: Deployment
metadata:
  name: transaction-service
  namespace: onlineakimbank
spec:
  replicas: 1
  selector:
    matchLabels:
      app: transaction-service
  template:
    metadata:
      labels:
        app: transaction-service
    spec:
      initContainers:
        - name: wait-for-eureka
          image: curlimages/curl:latest
          command:
            - sh
            - -c
            - 'until curl -s http://eureka-server:8761/eureka/apps > /dev/null; do echo "Waiting for Eureka Server..."; sleep 5; done'
      containers:
        - name: transaction-service
          imagePullPolicy: Always
          image: ghcr.io/akim1001/onlineakimbank/transaction-service:latest
          ports:
            - containerPort: 8085
          env:
            - name: SPRING_PROFILES_ACTIVE
              value: "prod"
      imagePullSecrets:
        - name: ghcr-secret
---
apiVersion: v1
kind: Service
metadata:
  name: transaction-service
  namespace: onlineakimbank
spec:
  selector:
    app: transaction-service
  ports:
    - port: 8085
      targetPort: 8085
