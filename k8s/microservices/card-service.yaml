apiVersion: apps/v1
kind: Deployment
metadata:
  name: card-service
  namespace: onlineakimbank
spec:
  replicas: 1
  selector:
    matchLabels:
      app: card-service
  template:
    metadata:
      labels:
        app: card-service
    spec:
      initContainers:
        - name: wait-for-eureka
          image: curlimages/curl:latest
          command:
            - sh
            - -c
            - 'until curl -s http://eureka-server:8761/eureka/apps > /dev/null; do echo "Waiting for Eureka Server..."; sleep 5; done'
      containers:
        - name: card-service
          imagePullPolicy: Always
          image: ghcr.io/akim1001/onlineakimbank/card-service:latest
          ports:
            - containerPort: 8083
          env:
            - name: SPRING_PROFILES_ACTIVE
              value: "prod"
      imagePullSecrets:
        - name: ghcr-secret
---
apiVersion: v1
kind: Service
metadata:
  name: card-service
  namespace: onlineakimbank
spec:
  selector:
    app: card-service
  ports:
    - port: 8083
      targetPort: 8083
