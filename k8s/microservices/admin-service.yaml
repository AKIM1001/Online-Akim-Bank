apiVersion: apps/v1
kind: Deployment
metadata:
  name: admin-service
  namespace: onlineakimbank
spec:
  replicas: 1
  selector:
    matchLabels:
      app: admin-service
  template:
    metadata:
      labels:
        app: admin-service
    spec:
      initContainers:
        - name: wait-for-eureka
          image: curlimages/curl:latest
          command:
            - sh
            - -c
            - 'until curl -s http://eureka-server:8761/eureka/apps > /dev/null; do echo "Waiting for Eureka Server..."; sleep 5; done'
      containers:
        - name: admin-service
          imagePullPolicy: Always
          image: ghcr.io/akim1001/onlineakimbank/admin-service:latest
          ports:
            - containerPort: 8088
          env:
            - name: SPRING_PROFILES_ACTIVE
              value: "prod"
      imagePullSecrets:
        - name: ghcr-secret
---
apiVersion: v1
kind: Service
metadata:
  name: admin-service
  namespace: onlineakimbank
spec:
  selector:
    app: admin-service
  ports:
    - port: 8088
      targetPort: 8088
