spring:
  config:
    import: vault://secret/prod/payment-service
  cloud:
    vault:
      uri: https://vault:8200
      authentication: kubernetes
      kubernetes:
        role: payment-service-prod
        service-account-token-file: /var/run/secrets/kubernetes.io/serviceaccount/token
      kv:
        enabled: true
        backend: secret
        application-name: prod/payment-service
      enabled: true