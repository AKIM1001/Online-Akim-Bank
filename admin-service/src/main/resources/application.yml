server:
  port: 8088

springdoc:
  swagger-ui:
    path: /swagger-ui.html

spring:
  application:
    name: admin-service
  threads:
    virtual:
      enabled: true

keycloak:
  use-resource-role-mappings: true

management:
  endpoints:
    web:
      exposure:
        include: prometheus